# Pareto Frontier Design

The Pareto report compares models across quality, latency, cost, safety, and reliability.

A model is dominated when another model is at least as good on quality, safety, and reliability while also being no slower and no more expensive.

Reports:

- `data/scorecards/pareto_frontier_report.csv`
- `data/scorecards/pareto_frontier_by_task_type.csv`
